# 开发状态 · 0.4.0-preview

本轮目标：在不改变核心战斗数值和存档格式的前提下，提高 Unity 聚落操作辨识度与远征战斗表现，并把首页和交战场景都纳入成品烟雾测试。

## 已完成验证

- Windows 原生程序成功编译。
- 94 项核心检查：包含原有经济/战斗/存档回归，以及全部建筑上限、主城升级解锁、超限不扣资源、拆除释放占地和名额、返还与仓储上限、主城保护、重复拆除、撤销不复活、拆除最后兵营后阻止出征、旧存档超限兼容、模型面与数值有效性、兵种说明完整性。
- 19 项程序交互检查：真实按钮/键盘处理路径，包含新建、模型屋顶拾取、移动、撤销/重做、首次出征教学、投兵、结束与返回、图鉴、拆除确认/取消/完成、帮助、最小窗口。
- 已输出并检查基地、进攻、结算、兵种图鉴、首次教学、拆除确认，以及 1100×760 小窗口图鉴；另输出 1920×1080 渲染画面。
- 交互测试直接调用本程序的事件处理并渲染界面；没有完成用户桌面上的人工鼠标试玩、不同电脑兼容性或持续性能测试。
- Unity 工程已由 Unity 6000.6.0f1 / URP 17.6.0 完成首次导入和脚本编译，生成并保存主场景、URP 配置、材质、项目设置、包锁文件及资源 `.meta`。
- Unity Windows x64 构建成功；最终日志包含本轮 `Build Finished, Result: Success` 和输出路径标记，构建脚本还会检查 exe、Data、UnityPlayer 与运行时目录是否齐全。
- Unity 成品已用独立测试存档真实启动，使用 D3D11 渲染 1440×900 基地画面并以退出码 0 自动结束；测试没有读写正式村庄存档。
- 0.4 表现层加入选中/放置反馈、投兵边界、平滑单位移动、三维弹道、受击闪光、治疗范围、冲击圈和摧毁碎片；没有改动规则层伤害、射程、速度或奖励。
- `tools/test-unity-player.ps1` 分别验证聚落与推进到交战阶段的成品画面，检查截图新鲜度、完成标记、退出码以及常见运行/材质异常。

## 技术决定

当前机器已安装并激活 Unity 6000.6.0f1（含 Windows Standalone Support），同时有 .NET Framework C# 编译器但没有独立 .NET SDK。因此继续使用同一份 Core 源码，同时提供轻量原生试玩宿主与 Unity 3D 宿主。原生预览器不承担正式网络架构。

Core 不依赖 Unity，位置使用每格1000单位的整数坐标、20Hz固定步长。寻路是四方向带墙体成本的 A*；部队实际拆墙后才能穿过对应路径。选目标有兵种偏好，但尚未做整条路径成本参与目标排序，也没有部队避让。

关卡预设容易，主要用于验证行为。自动化全兵力进攻可以通过三个关卡，不能据此宣称数值平衡完成。尚未设置失败次数、难度自适应或付费设计。

`Core/ModelGeometry.cs` 定义引擎无关的三维面和顶点色，程序化生成 7 类建筑的三级模型及 4 类兵种。原生预览器投影、遮面、排序、打光后以 2 倍采样缓存模型精灵；Unity `ModelViews.cs` 从同一几何生成网格，使用 URP 顶点色光照材质。这样后续改善模型可同时影响两个宿主。原生地形单独缓存，未接入骨骼动画。

建筑上限统一由 `Rules.BuildLimit` 按议事堡等级决定，UI 只是展示，实际限制在 `GameSession.Build`。不在读档校验中强制新数量上限，避免删除旧版建筑。拆除先收取已有收益，再按建设和历次升级总投入返还 50%；保护议事堡，清空移动历史，持久化仍使用原来的 XML 版本 1。

图鉴和战斗说明读取真实的 `TroopSpec` 数值，不使用另一套展示数值。教学引导铁卫吸引火力、破城手开墙、先锋跟进、游侠后排输出；首次出征教学按进程显示一次，不新增存档字段。模态面板暂停本地战斗，并阻止点击穿透。

## 下一步顺序

1. 人工走完 0.4 Unity 完整流程：重点确认带模型的建造预览、连续铺墙、选中圈、治疗范围和高密度战斗时的可读性。
2. 在现有程序化运动上补正式骨骼动画、命中特效和音效，并针对中低端 Windows 设备建立帧率预算。
3. 在共享程序化模型基础上制作正式美术样板，统一角色比例、材质、光照、特效和 UI 规范；当前细节模型仍不等于正式美术完成。
4. 加入部队避让和更完善的目标选择，调整三关难度；之后扩充到 10 关、第二种法术与进阶引导。
5. 设计独立在线进度，接入账号、基地快照、匹配、版本化战斗凭据、服务端复算和事务结算。

## 未解决限制

- Unity 自动构建和首页渲染烟雾测试已通过，但还没有完成整套 Unity 人工交互回归、三场远征逐关验收或其他电脑兼容性测试。
- `tools/build-unity.ps1` 提供 Check / Prepare / Build / Package，固定工程版本、独立日志、失败退出码和本轮成功标记检查；打包会排除 Unity 标记为不可发布的备份目录。
- 原生试玩的布局撤销只覆盖单个建筑移动；没有多选框选或批量撤销建造。
- 资源依赖本地时间，存档未加密；不应用于联网排名或付费经济。
- 同环境复现测试不是跨平台确定性证明，也没有完成命令文件导入回放功能。
- 本地只保留上一份存档；未实现跨进程存档写锁或云同步。
- 没有正式性能指标；不能把截图生成耗时当作帧率。
